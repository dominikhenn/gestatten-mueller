<div class="modular-wrapper modular-wrapper--welcome">
    <section class="container">
        <h1>{{ page.title }}</h1>
        {{ page.content|raw }}
    </section>
    {% include 'partials/page/separator.html.twig' with {direction: 'right', color: 'secondary'} %}
</div>
