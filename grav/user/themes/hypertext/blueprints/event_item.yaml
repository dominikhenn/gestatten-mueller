# Functionally identical to a collection, this only exists
# to make compatibility with other themes easier.

extends@: default

form:
    fields:
        header.event_date:
            type: datetime
            label: PLUGIN_ADMIN.DATE
            toggleable: false
            help: PLUGIN_ADMIN.DATE_HELP
            validate:
                required: true
