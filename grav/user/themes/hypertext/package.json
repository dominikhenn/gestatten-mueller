{
  "name": "hypertext",
  "version": "1.0.0",
  "description": "[Hypertext](http://hypertext.artofthesmart.com) is a theme for [Grav CMS](google.com) that prioritizes speed and simplicity.  It does not require (nor does it include) any Javascript and it keeps styles to a bare minimum.  This ensures a fast, consistent user experience across browsers and devices.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass --include-path scss scss/layout.scss css/layout.min.css --output-style compressed",
    "watch": "nodemon -e scss -x \"npm run build\""
  },
  "author": "Dominik Henn",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.13.1",
    "nodemon": "^1.19.1"
  }
}
